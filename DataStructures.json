//Request Structure
{
    serviceRoute: {
        domain: "Domain name defined in mapping",
        service: "Service name defined in mapping"
        uuid: "Auto injected from client during request handling"
    },
    params: {
        key: "Data to pass in"
    }
}

//Response Structure
{
    uuid: "Request UUID",
    type: "receipt" || "response" || "error",
    success: true || false,
    //If response
    response: Object,
    //If success === false
    error: {
        origin: "Origin class throwing this error"
        code: "Error code", //For application use
        msg: "Error message" //UI friendly
    }
}



var request = null;
request = {
    serviceRoute: {
        domain: "led",
        service: "getAvaliableControllers"
    }
}
webSocket.send(request);

request = {
    serviceRoute: {
        domain: "led",
        service: "addController"
    },
    params: {
        deviceName: "Lagute-0001"
    }
}
webSocket.send(request);

request = {
    serviceRoute: {
        domain: "led",
        service: "sendCommand"
    },
    params: {
        device: "Lagute-0001",
        command: "on"
    }
}
webSocket.send(request);

request = {
    serviceRoute: {
        domain: "led",
        service: "sendCommand"
    },
    params: {
        device: "Lagute-0001",
        command: "setColour",
        colour: "00FF00"
    }
}
webSocket.send(request);

request = {
    serviceRoute: {
        domain: "led",
        service: "sendCommand"
    },
    params: {
        device: "Lagute-0001",
        command: "setBrightness",
        brightness: "50"
    }
}
webSocket.send(request);

request = {
    serviceRoute: {
        domain: "led",
        service: "sendCommand"
    },
    params: {
        device: "Lagute-0001",
        command: "transitionToColour",
        colour: "FF00FF",
        pauseInterval: "3",
        incrementInterval: "1"
    }
}
webSocket.send(request);

request = {
    serviceRoute: {
        domain: "led",
        service: "sendCommand"
    },
    params: {
        device: "Lagute-0001",
        command: "off"
    }
}
webSocket.send(request);