//Request Structure
{
    "serviceRoute": {
        "domain": "Domain name defined in mapping",
        "service": "Service name defined in mapping",
        "uuid": "Auto injected from client during request handling"
    },
    "params": {
        "key": "Data to pass in"
    }
}

//Response Structure
{
    "uuid": "Request UUID",
    "type": "receipt" || "response" || "error",
    "success": true || false,
    //If response
    "response": Object,
    //If success === false
    "error": {
        "origin": "Origin class throwing this error",
        "code": "Error code", //For application use
        "msg": "Error message", //UI friendly
    }
}

// Network
var params = {
    "ipAddress": "192.168.1.3",
    "timeout": "3000"
}
lumitron.request.send("network", "isIpReachable", params);

var params = {
    "ipAddress": "192.168.1.3"
}
lumitron.request.send("network", "getMacAddress", params);

// Controller registration
lumitron.request.send("led", "getAvailableControllerModels");

lumitron.request.send("led", "getRegisteredControllers");

var params = {
    "model": "Lagute",
    "name": "Lagute-0001",
    "ipAddress": "192.168.1.3"
}
lumitron.request.send("led", "registerController", params);

var params = {
    "name": "Lagute-0001",
}
lumitron.request.send("led", "deregisterController", params);

// Led manipulation
var params = {
    "device": "Lagute-0001",
    "command": "queryStatus"
}
lumitron.request.send("led", "sendCommand", params);

var params = {
    "device": "Lagute-0001",
    "command": "on"
}
lumitron.request.send("led", "sendCommand", params);

var params = {
    "device": "Lagute-0001",
    "command": "off"
}
lumitron.request.send("led", "sendCommand", params);

var params = {
    "device": "Lagute-0001",
    "command": "setColour",
    "colour": "00FF00"
}
lumitron.request.send("led", "sendCommand", params);

var params = {
    "device": "Lagute-0001",
    "command": "setBrightness",
    "brightness": "50"
}
lumitron.request.send("led", "sendCommand", params);

var params = {
    "device": "Lagute-0001",
    "command": "transitionToColour",
    "colour": "FF00FF",
    "pauseInterval": "3",
    "incrementInterval": "1"
}
lumitron.request.send("led", "sendCommand", params);

//Music
var params = {
    "file": "C:\\Fresh.mp3"
}
lumitron.request.send("music", "load", params);

lumitron.request.send("music", "play", null);

lumitron.request.send("music", "pause", null);

lumitron.request.send("music", "stop", null);

//Test pattern
setInterval(function() {
    var params = {
        "device": "Lagute-0001",
        "command": "setColour",
        "colour": "0000FF"
    }
    lumitron.request.send("led", "sendCommand", params);
}, 3000);
setTimeout(function() {
    setInterval(function() {
        var params = {
            "device": "Lagute-0001",
            "command": "setColour",
            "colour": "FF0000"
        }
        lumitron.request.send("led", "sendCommand", params);
    }, 3000);
}, 1000);
setTimeout(function() {
    setInterval(function() {
        var params = {
            "device": "Lagute-0001",
            "command": "setColour",
            "colour": "00FF00"
        }
        lumitron.request.send("led", "sendCommand", params);
    }, 3000);
}, 2000);
